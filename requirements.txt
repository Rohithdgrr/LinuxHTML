# requirements.txt - Python dependencies for LinuxHTML
# All versions pinned per README-1.md:2118 / versions.lock:python_deps
# Install: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# Verify: python3 tools/verify_versions.py
#
pytest==8.1.1
jinja2==3.1.3
pyyaml==6.0.1
# playwright & pytest-playwright are optional, required for browser tests (Phase 7+)
# Install manually when needed: pip install pytest-playwright==0.4.4 playwright==1.42.0
# NOTE: On Python 3.14 Windows, greenlet may fail to build; use Python 3.11/3.12 for full browser tests
# or pin to compatible versions in versions.lock per README-1.md:2113

# Optional but pinned for tooling
# Used by tools/pack.py and tools/bench/
# Add new deps ONLY with versions.lock update in same PR per README-1.md:2113
